export enum LockerSize {
  S = 'S',
  M = 'M',
  L = 'L',
}

export enum DoorStatus {
  ABIERTO = 'abierto',
  CERRADO = 'cerrado',
}

export enum OccupancyStatus {
  OCUPADO = 'ocupado',
  VACIO = 'vacio',
}
